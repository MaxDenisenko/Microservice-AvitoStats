import { expect } from "chai";
import { ClickHouse } from 'clickhouse'

export default Test;

function Test({ ClickHouseManager, db, health, config }) {
	it(`Проверка экспорта в CLickHouse`, async (done) => {
		done()


	});
}